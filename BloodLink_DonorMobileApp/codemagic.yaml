workflows:
  build_ios:
    name: Build iOS
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - flutter pub get
      - cd ios && pod install --repo-update
      - cd ..
      - flutter build ios --release --no-codesign
